# Database
DATABASE_URL=postgresql://novaguard:novaguard@localhost:5432/novaguard

# Database Connection Pool (Production Settings)
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30
DATABASE_POOL_RECYCLE=3600

# JWT
JWT_SECRET=troque_este_segredo_em_producao_use_openssl_rand_hex_32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# API
API_HOST=0.0.0.0
API_PORT=8080
API_RELOAD=true

# CORS
CORS_ORIGINS=http://localhost:5000,https://*.replit.dev

# Environment (development | production)
ENVIRONMENT=development

# Docker Infrastructure
DOCKER_NETWORK=novaguardian
PIHOLE_BASE_PORT=8081
PIHOLE_IMAGE=pihole/pihole:latest
PIHOLE_SUBNET=172.20.0.0/16
PIHOLE_IP_START=10

# dnsdist Configuration
DNSDIST_CONFIG_PATH=/etc/dnsdist/dnsdist.conf
DNSDIST_CONTAINER=novaguardian-dnsdist

# DNS Infrastructure (Development)
PUBLIC_IP_DEMO=203.0.113.10
PIHOLE_DEMO_CONTAINER=pihole-demo
DNSDIST_LISTEN_PORT_DEV=5353
